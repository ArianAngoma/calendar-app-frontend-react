const initialState = {
    checking: true,
    // uid: null,
    // name: null,
    // color: null
}

export const authReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}